function palindrome(str){
    // lower case and remove alpha numeric characters
    str = str.toLowerCase().replace(/[\W_]/g,"")

    let left = 0;
    let right = str.length -1

    while(left < right){
        if(s[left] !== s[right]){
            
        }
    }
}

module.exports = palindrome